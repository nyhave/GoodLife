# DayTrading

A day trading application built with React. It uses CDN versions of React and does not require any build step.

The page includes a minimal [Progressive Web App](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps) setup with a manifest and service worker for basic offline caching.

## Usage
Open `index.html` in your browser.
